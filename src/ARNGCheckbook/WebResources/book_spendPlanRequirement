function openSpendPlan(formContext) {
    // Centered Dialog
    var pageInput = {
        pageType: "custom",
        name: "book_pagenpmspendplan_7fa13",
        entityName: "book_requirementfundings",
        recordId: formContext.data.entity.getId().replace(/[{}]/g, ""),
    };
    var navigationOptions = {
        target: 2,
        position: 1,
        width: { value: 90, unit: "%" },
        height: { value: 70, unit: "%" },
        title: "Spend Plan (Requirement)"
    };
    Xrm.Navigation.navigateTo(pageInput, navigationOptions)
        .then(
            function () {
                // Called when the dialog closes
                formContext.data.refresh();
            }
        ).catch(
            function (error) {
                // Handle error
            }
        );

}