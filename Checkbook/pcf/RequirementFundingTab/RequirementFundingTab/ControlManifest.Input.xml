<?xml version="1.0" encoding="utf-8" ?>
<manifest>
  <control namespace="ARNGCheckbook" constructor="RequirementFundingTab" version="0.2.2"
           display-name-key="RequirementFundingTab"
           description-key="Composite Funding tab for a Requirement. Sticky FY picker drives a Requirement Details (rank) section, a read-only Requirement Fundings list, a Funding by Prioritizations grid, and a Funding by Requirement Details grid. Sections show/hide based on whether the Requirement has Prioritizations and/or Requirement Details. Host on a subgrid in a one-column tab; the dataset rows are ignored — the control reads the parent Requirement ID from page context and drives everything via WebAPI."
           control-type="virtual" >
    <external-service-usage enabled="false">
    </external-service-usage>
    <!-- Dataset host. The dataset rows are ignored; the control reads the parent
         Requirement ID from page context. Bind to any view (cheapest: a view that
         returns 0 rows). Place the subgrid in a one-column section in its own tab. -->
    <data-set name="records" display-name-key="Records"
              description-key="Host dataset (rows are ignored). Bind to any view on any table." />
    <resources>
      <code path="index.ts" order="1"/>
      <css path="css/RequirementFundingTab.css" order="1" />
      <platform-library name="React" version="16.14.0" />
      <platform-library name="Fluent" version="9.46.2" />
    </resources>
    <feature-usage>
      <uses-feature name="WebAPI" required="true" />
      <uses-feature name="Utility" required="true" />
    </feature-usage>
  </control>
</manifest>
